package FilterLogsForAnomlies;

public class Filter {
	
	/* Use the 50% most similar traces 
	 * Generate rule from them
	 * Apply this rules on the remaining traces to determine which stick out and support the least amount of rule 
	 * e.g. the ten percent of traces that support the least amount of rules
	 * 
	 */

}
